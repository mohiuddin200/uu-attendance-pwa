VITE_CONVEX_URL=

# Convex backend environment variables:
# Set these with `npx convex env set NAME value`.
# INITIAL_CR_EMAILS can contain comma-separated emails.
INITIAL_CR_EMAILS=your-cr-email@uttara.ac.bd
JWT_PRIVATE_KEY=
JWKS=
